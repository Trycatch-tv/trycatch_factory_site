---
const info = {
  title: "Llamada de exploración gratuita",
  description:
    "Si estás buscando inspiración para tu página o deseas discutir requisitos específicos con nosotros, por favor, déjanos tus datos y nos comunicaremos contigo para brindarte la ayuda que necesitas en la construcción de tu sitio web.",
};
---

<div
  class="md:container md:mx-auto w-screen rounded-none overflow-hidden min-h-screen flex flex-col justify-center"
>
  <div class="flex w-full flex-col md:flex-row">
    <div class="w-full md:w-1/2">
      <h1 class="text-white text-4xl font-bold text-left p-9  max-sm:text-2xl md:text-2xl  lg:text-4xl ">
        {info.title}
      </h1>
      <p class="text-white text-2xl p-11 leading-10 max-sm:text-xl md:text-xl ">
        {info.description}
      </p>
    </div>

    <!-- Responsive en 1280 -->

    <div class="w-full md:w-1/2 justify-center">
      <form class="w-full max-w-2xl p-10 max-md:max-w-3xl">
        <div class="flex flex-wrap -mx-3 mb-6">
          <!-- Input nombre -->
          <div class="w-full md:w-1/2 px-3 mb-6 md:mb-0">
            <label
              class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
              for="grid-first-name"
            >
              Nombre
            </label>
            <input
              class="appearance-none block w-full bg-gray-200 text-tcf-blue  rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white"
              id="grid-first-name"
              type="text"
              placeholder="Nombre"
            />
          </div>

          <!-- Input apellidos -->
          <div class="w-full md:w-1/2 px-3">
            <label
              class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
              for="grid-last-name"
            >
              Apellidos
            </label>
            <input
              class="appearance-none block w-full bg-gray-200 text-tcf-blue  rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500"
              id="grid-last-name"
              type="text"
              placeholder="Apellidos"
            />
          </div>
        </div>

        <!-- Input email -->
        <div class="flex flex-wrap -mx-3 mb-6">
          <div class="w-full px-3">
            <label
              class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
            >
              Email corporativo
            </label>
            <input
              class="appearance-none block w-full bg-gray-200 text-tcf-blue  rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white focus:border-gray-500"
              id="grid-password"
              type="email"
              placeholder="example@mail.com"
            />
            <p class="text-gray-600 text-xs italic"></p>
          </div>
        </div>

        <!-- Input celular -->
        <div class="flex flex-wrap -mx-3 mb-2">
          <div class="w-full md:w-full px-3 mb-6 md:mb-0 lg:w-1/3">
            <label
              class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
              for="grid-zip"
            >
              Número de celular
            </label>
            <input
              class="appearance-none block w-full bg-gray-200 text-tcf-blue  rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500"
              id="grid-zip"
              type="text"
              placeholder="+57 300 123 4567"
            />
          </div>

          <!-- Input empresa -->
          <div class="w-full md:w-full px-3 mb-6 md:mb-0 lg:w-1/3">
            <label
              class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
              for="grid-zip"
            >
              Nombre de la empresa
            </label>
            <input
              class="appearance-none block w-full bg-gray-200 text-tcf-blue  rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500"
              id="grid-zip"
              type="text"
              placeholder="Nombre de la empresa"
            />
          </div>

          <!-- Input ciudad -->
          <div class="w-full md:w-full px-3 mb-6 md:mb-0 lg:w-1/3">
            <label
              class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
              for="grid-zip"
            >
              Ciudad
            </label>
            <input
              class="appearance-none block w-full bg-gray-200 text-tcf-blue rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500"
              id="grid-zip"
              type="text"
              placeholder="Ciudad"
            />
          </div>
        </div>

        <!-- Text area -->

        <div class="flex flex-wrap -mx-3 m-6 max-sm:px-0">
          <div class="w-full p-3">
            <label
              class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
            >
              Déjanos tu mensaje y pronto nos contactaremos contigo
            </label>
            <textarea
              class="appearance-none block w-full bg-gray-200 text-tcf-blue  rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white focus:border-gray-500"
              id="grid-password"
              placeholder="escribe aquí"></textarea>
            <p class="text-gray-600 text-xs italic"></p>
          </div>
        </div>

        <!-- Boton enviar -->

        <div class="flex justify-end max-sm:justify-center">
          <button
            class="bg-gray-700 hover:bg-blue-500 text-white font-bold py-2 px-4 rounded m-8 text-xl max-sm:px-8"
          >
            Enviar
          </button>
        </div>
      </form>
    </div>
  </div>
</div>
